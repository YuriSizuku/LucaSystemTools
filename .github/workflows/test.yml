name: test

on: [push]

jobs:
  show_secrets:
    runs-on: ubuntu-latest
    steps:
      - shell: bash
        env:
           test_secret = ${{ secrets.TEST_SECRET }}
        run: |
          echo $test_secret
